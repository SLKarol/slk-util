туду

- Работать с процессом
- ERROR_MAIN научиться обрабатывать
- В "Зайти на страницу" добавить что нужно зайти к ботам

## Зайти. Логика работы.

Принципы:
Всегда зайти только к одному автору. Поэтому вести список посещённых. botVisitedEntered. Предлагаю такую структуру : Map<login:string, {visited: string(datetime)[]}>

Запрашивать список позавчерашнего дня. Получить случайно раздел. Запомнить его в Set
Если автор не в читанных (Это условие в отдельный метод), то:
открыть в броузере его произведение. Ждать 5 секунд.
Когда страница кончилась, переходить к другому случайному разделу, не такому же.

interface VisitPoemsByAuthor {
url: string;
dateTime: string
}

05.04
План такой:

1. Сделать сохранение в файл trackerVisitAuthors.json . Туда будешь записывать json. Структура такая: Map<authorId, VisitPoemsByAuthor[]> .
2. Выполняется функция:
   2.1 Выбрать случайный раздел
   2.2 Считать произведения из него
   2.3 Произведения forEach
   2.3.1 Если автора нет в trackerVisitAuthors, то открыть произведение и записать в логи
   (Вероятно я сделаю проверку стихов от одного автора)

Сделать на таймауты сигнал
Подумать над периодом. Подумать над возможностью сделать период случайным числом. От 10 до 20 минут.
Давай-ка я прочитанное буду удалять.
