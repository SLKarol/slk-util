import { Container } from "@mantine/core";

export const SettingsButtonRun = () => {
  return (
    <Container m={0}>
      <button title="Сохранит и запускает">Сформировать строку</button>
    </Container>
  );
};
